# conda env create -f environment.yml
name: regression
channels:
  - conda-forge
  - defaults
dependencies:
  - python==3.8.2
  - pip
  - jupyter
  - pip:
      - black==19.10b0
      - flake8
      - isort
      - seaborn
